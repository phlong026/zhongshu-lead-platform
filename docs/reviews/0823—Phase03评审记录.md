# 0823 — Phase 03（P2/P3 批次）综合评审记录：邀请绑定整改复核与新发现

| 项 | 内容 |
|---|---|
| 评审日期 | 2026-08-23 |
| 评审对象 | 提交区间 `919fe1c..661606a`（`5a5c538` 安全整改、`ec2eac2` P2-5 失败审计、`67cfde9` P3 批次、`b0d6716` 通道码透传、`af59d06` I15/P2-1 联动、`661606a` I16 断言加固；另含 `86a18e1` 产物清理） |
| 评审基线 | 《0822—Phase01评审记录》（C1/C2、I1–I21）与《0822—Phase02评审记录》（P2-1–P2-5、P3-1–P3-10）全部未决项 |
| 评审方式 | 主审（逐文件 diff + 提交态逐行核验 + 关键主张实证）+ 3 个并行专家子代理（测试、可维护性、红队）+ Codex 独立对抗评审 |
| 执行环境 | 干净 detached worktree（661606a 检出），主仓 `.venv` Python 3.11 / pytest 9.1.1；主仓工作区另有并行会话未提交改动（auth.py 等 4 文件），不属本评审对象 |
| 结论 | **有保留通过**：C1/C2 与绝大多数 P2/P3 项已真实修复且有测试锁定；但 **P2-2、P2-4 完全未修复**，I15/I16 落地但防护可绕过/未接入，且新发现 2 个 P1 级安全与测试有效性问题（限流 IP 信任、outbox 凭据入库） |

---

## 1. 结论摘要

- **Phase01 清单 23 项（C1/C2 + I1–I21）**：18 项已修复，I12 部分修复（文档化接受），I15/I16 部分修复（详见 §2），I19 未修复反加重，I21 未处理。
- **Phase02 清单 15 项（P2-1–P2-5、P3-1–P3-10）**：10 项已修复，P2-3 部分修复，P3-6/P3-8 部分修复，**P2-2、P2-4 未修复**，P2-1 主体修复但 start 阶段存在同类新缺口（N6）。
- **新发现 N1–N14**（§4）：2 个 P1（限流 IP 信任链、outbox `last_error` 凭据泄漏）、6 个 P2、测试有效性 1 项 P1（PG 并发 e2e 镜像漂移，双源确认）及若干 P3。
- **测试证据**：批次相关 8 个测试文件 109 passed / 5 skipped（PG 专项按设计 skip）；全套件 539 passed / 7 skipped / 1 deselected——该 deselected 例与「单文件运行必失败」均为**存量**测试基建缺陷（N14），涉及文件本批未动（`git diff 919fe1c..661606a -- <四文件>` 为空）。

## 2. Phase01 清单整改核验（对照《0822—Phase01评审记录》）

| 项 | 判定 | 证据（661606a 提交态） |
|---|---|---|
| C1 return_url 开放重定向 | **已修复** | `routers/auth.py:170-183` 前缀白名单（非 `/h5/` 一律回落）+ 控制字符/反斜杠拒绝；`test_confirm_start_neutralizes_control_char_return_urls`、`test_wechat_start_neutralizes_control_char_return_urls`、合法值保留测试三例锁定 |
| C2 已绑定用户重登锁死 | **已修复** | `h5/app.js` `bindWechatLogin` 无邀请改走 legacy `/wechat/start`（`invite=` 参数被契约测试禁带）；`bound_other` 文案不再承诺不存在的换绑；`test_bound_user_can_relogin_without_invite_via_legacy_start` 端到端锁定 |
| I1 登录门禁无超时兜底 | **已修复** | `status-pages-v13.js` `zsLoadInvitePreview` AbortController 8 秒超时 + 「邀请核验失败」可重试路径 |
| I2 预览 catch 一律判死 | **已修复** | 5xx 抛可重试错误（附「重新核验邀请」按钮），4xx/业务码才置 `inviteInvalid=1` 锁死 |
| I3 `?v=` 未递增 | **部分修复** | 本批受影响脚本已递增（h5/admin `app.js`、`status-pages-v13.js` → `20260822-p2`）；但 `safe-html.js`/`enhancements.js`/`points-enhancements.js` 仍完全无版本参数（与 P2-3 同源残留，见 §3） |
| I4 首绑/重登审计不可区分 | **已修复** | 生产 callback 按 bind intent 分流 `WECHAT_BIND`（含 `invite_id` metadata + ip/ua）与 `WECHAT_OAUTH_LOGIN`；与 mock 通道对齐；`test_callback_audit_distinguishes_bind_from_relogin` 锁定 |
| I5 AB-BA 死锁 | **已修复** | `auth_service.py` `login_or_bind_wechat` 改为 validate（无锁）→ company `FOR UPDATE` → `_consume_invite`，与创建侧「公司→邀请」同序；PG e2e 同步更新 |
| I6 M2 锁定层级在服务层 | **已修复** | `test_callback_failure_writes_audit_and_discards_partial_writes` 以基线计数断言失败回调后无孤儿 User/UserRole 漏入（HTTP 路径级断言，正是 I6 诉求） |
| I7 停用公司竞态 | **已修复** | 行锁后复核 `status=='ACTIVE'` + 占用 UPDATE WHERE 追加 `status=='ACTIVE'` 双保险；`test_binding_after_company_disable_lands_on_status_page` 锁定 |
| I8 撤销无状态校验 | **已修复** | `revoke_invite` 行锁读取 + revoked/used/expired 三态 409（`INVITE_ALREADY_REVOKED/USED/EXPIRED`）；`test_revoke_invite_validates_lifecycle_status` 锁定。**但**并发 e2e 用镜像逻辑代替生产调用，见 N4 |
| I9 门禁扫描粒度 | **已修复** | 按 `index.html` 实际加载脚本集扫描；onclick 计数限定 `bindWechatLogin` 函数体；增强脚本代码态禁引用 confirm-start 端点 |
| I10 外链门禁绕过面 | **已修复** | W3C 豁免收窄 `vendor/`；协议相对 URL 正则检测；HTML 内联脚本补检。`atob`/字符串拼接构造仍可绕过（原有已知边界，维持现状） |
| I11 DISABLED 未参数化 | **已修复** | `test_non_active_company_cannot_create_invite(db, status)` 参数化 |
| I12 bearer 意图 | **部分修复（文档化接受）** | `_BINDING_STATE_TTL_MINUTES=5` 显式收紧 + 签发侧审计记 ip/ua（供事后对照）；openid 预授权、confirm/callback 差异自动比对未做——与 Phase01「文档化风险」口径一致 |
| I13 双参语义分裂 | **已修复** | `validate_invite` 与 `_consume_invite` AND 语义对齐（token 解析不到即拒、双参必须同源）；`bind_wechat_by_invite` docstring 显式声明测试缝；mismatched 测试锁定 |
| I14 同 openid 并发 IntegrityError | **未修复** | `auth_service.py` `login_or_bind_wechat` 仍为先查后插，无 IntegrityError 捕获/回滚重读；与 P2-4② 同根（Codex#4 + 红队双确认，见 N5） |
| I15 匿名 confirm-start 限流 | **部分修复（防护可绕过）** | 限流已落地（invite+IP 双键、60s/10 次滑窗）且有测试；但 IP 取自客户端可控 `x-real-ip`、进程内无锁字典、多 worker 放大（N1）、垃圾 invite 键无界增长（N8） |
| I16 primary_user_id 一致性 | **部分修复（未接入）** | `binding_integrity.py` 五类脏形态核查 + 5 测试；但**零生产调用点**（grep 仅测试引用），未接启动检查/发布门禁/告警（N2）；红队另指出 READ COMMITTED 下并发误报/漏报窗口 |
| I17 过期 bind-intent 负例 | **已修复** | `test_expired_bind_intent_state_is_rejected_at_callback` |
| I18 TTL 隐式依赖 | **已修复** | `_BINDING_STATE_TTL_MINUTES` 常量显式传参；`test_confirm_start_state_ttl_is_tightened` |
| I19 测试 helper 复制 | **未修复（加重）** | 本批 `test_binding_integrity.py` 整段复制 `_company_body`、`test_notifications.py` 手写 admin 登录而不用既有 `_admin_headers`、`test_wechat_oauth.py` 新增 11 处三行样板（N13） |
| I20 SQLite 伪并发 | **已修复** | `test_parallel_invite_callbacks_still_consume_exactly_once` 加 `Barrier(2)` 并诚实改名/注释（锁语义委托 PG e2e，锁定调度无关不变量） |
| I21 空库 ValueError 非 skip | **未处理** | `_postgres_factory` 未加 schema/角色缺失检测；单跑该文件对空库仍会失败而非 skip（低优先） |
| 范围外：waivers 续期重置 | **已修复** | `check_security_gate.py` `first_waived_on` + 90 天累计跨度硬上限；`test_waiver_total_span_hard_cap_blocks_perpetual_renewal` 锁定 |
| 范围外：OCI 回退弱化（置信 4） | **未跟进** | 维持 Phase01「需结合部署侧单独评估」的观察状态 |

## 3. Phase02 清单整改核验（对照《0822—Phase02评审记录》）

| 项 | 判定 | 证据（661606a 提交态） |
|---|---|---|
| P2-1 5xx 折叠成 302 | **已修复（callback 范围）** | `WECHAT_NOT_CONFIGURED/UNAVAILABLE/FAILED` 入 `_H5_AUTH_ERROR_CODES` 透传，H5 给「稍后重试」文案；`_WECHAT_CHANNEL_FAILURE_CODES` 精确告警（`test_callback_upstream_failure_keeps_specific_code_and_alerts`）；未知码仍折叠 `AUTH_FAILED`。**新缺口**：start/confirm-start 阶段通道故障仍裸 JSON 且审计顺序错误（N6） |
| P2-2 OWNER/FINANCE 可见必 403 按钮 | **未修复** | `admin/app.js:46` 「邀请」「邀请记录」仍对每行无条件渲染；同文件 `can()` helper 已存在并用于积分列/日历，唯独邀请入口未用；后端 API 仍 `require_permissions("*")`。OWNER/FINANCE 点击依旧必 403 |
| P2-3 `?v=` 递增 | **部分修复** | 受影响脚本已递增；`safe-html.js`/`enhancements.js`/`points-enhancements.js` 仍无版本参数机制，下次变更将复现同问题 |
| P2-4 「不出现裸 JSON」两缺口 | **未修复** | ① `wechat_callback` 签名仍 `code: str, state: str` 必填 query，缺参在 try 之前被 FastAPI 422 JSON 拒绝（`routers/auth.py:471-473`）；② IntegrityError 不在 `except AppError` 内，全局处理器回 409 JSON（同 I14，N5） |
| P2-5 失败路径零审计 | **已修复（AppError 范围）** | `WECHAT_OAUTH_CALLBACK_FAILED` 携 flow/failure_class/reason_code/display_code + ip/ua；rollback 丢弃半途写入；审计持久化失败降级 `logger.exception` 且维持 302（3 个测试锁定）。**边界**：非 AppError 异常绕过审计（N5）、失败审计自身无节流（N11） |
| P3-1 错误码双清单同步 | **已修复** | `test_h5_auth_error_codes_stay_in_sync_with_backend_whitelist` 双向集合比较（后端 import 真源 + 前端解析键集）+ security⊆transparent 断言；残留：CTA 检查硬编码三码未用 `_WECHAT_CHANNEL_FAILURE_CODES`（N12/N13） |
| P3-2 回落分支负例 | **已修复** | `test_callback_unknown_error_code_collapses_to_auth_failed`；`test_callback_wechat_failure_details_never_leak` 断言 errmsg 不入 URL/日志 |
| P3-3 limit(50) 误导文案 | **已修复** | 弹窗文案改「默认显示最近 50 条记录，更早记录仍在系统完整保留……」（诚实方案，未做分页） |
| P3-4 innerHTML 双轨 | **已修复** | `renderAuthError` 走 `zsSetSafeHtml`；契约断言 `innerHTML` 不得回退 |
| P3-5 CTA 错误指引 | **已修复** | noCta/homeCta/默认三分化；契约断言锁定 |
| P3-6 USED 过度归因 | **部分修复** | 仅 `used_at` 最近一条关联主账号名；但取值仍是**当前** `company.primary_user_id` 的名字而非当时使用者——换绑/人工修复后历史归因仍错（Codex#7，N9）；`WECHAT_BIND.metadata.invite_id` 已可关联真实用户但列表未用 |
| P3-7 撤销流程重复 | **已修复** | 共享 `revokeInvite(inviteId,{button,onDone})`，文案统一「该邀请」 |
| P3-8 局部导入 | **部分修复** | `AppError` 5 处已提升模块顶部；`WechatOAuthClient`×3、`RedirectResponse`×2 局部导入残留 |
| P3-9 列表测试缺口 | **已修复** | 404 / 无 `*` 权限 403 / `expires_at==now` 边界 / 降序+50 上限 / P3-6 归因断言均已补 |
| P3-10 过程产物 | **已修复** | `86a18e1` 清理 `.omx` 并加忽略；本批无新产物。「评审完成前合入」的顺序问题依旧（本批同样先合入后评审，流程注记） |

## 4. 新发现清单（N1–N14）

严重度：P1 为安全/数据缺陷或测试有效性失守；P2 为应整改；P3 为改进项。置信度 1-10。标注「多方确认」= 至少两个独立评审通道同结论。

### P1

| # | 位置 | 问题 | 置信 | 来源 |
|---|---|---|---|---|
| N1 | `routers/auth.py:39`（`_request_ip`）+ `:49-65`（限流） | **限流 IP 分量信任客户端可控的 `x-real-ip`，且计数为进程内无锁字典**：应用层零代理可信校验，防护完全依赖部署拓扑恰好由 nginx 覆写该头；直连 uvicorn/换 LB/异构代理时轮换该头即给每请求配新桶，彻底绕过 I15 限流并无限刷 `INVITE_CONFIRM_START` 审计；审计 `ip_address` 同时变成攻击者任意指定值，污染 I12/P2-5 的挪用追溯证据；多 worker 各自独立额度、同进程并发读改写丢计数；`test_auth_company.py:587` 甚至把「换 `x-real-ip` 后继续成功」锁成正确行为。修复：按可信代理 CIDR 解析 XFF 最右非可信跳或统一 `request.client.host`（uvicorn `--proxy-headers`）；计数迁共享存储；补「伪造头不得获得新桶」负例 | 10（实证：`_request_ip` 头优先，双源） | Codex+红队（双确认） |
| N2 | `services/binding_integrity.py`（全文件） | **I16「修复」是无生产调用点的报告函数**：未接入启动检查、发布门禁、健康检查、定时告警或绑定入口；除测试外零调用者。`SHARED_PRIMARY`/悬挂主账号/公司错配仍可原样上线；`PRIMARY_WITHOUT_IDENTITY` 定为 warning 使「无微信身份、无法登录、无法再邀请」的公司仍判 `valid=True`。修复：接入强制发布/启动门禁与运行时告警，对目标公司在邀请/绑定前 fail closed；用唯一约束/外键消除共享与悬挂态 | 10（grep 实证） | Codex |
| N3 | `services/outbox_worker.py:51`（`item.last_error = str(exc)`）+ `integrations/wechat.py:109-110/:62` + `routers/notifications.py:58` | **微信凭据可随异常原文持久化并回显运营端**：`send_scene_message` 把 `access_token` 拼进 URL 查询串且 `raise_for_status()`，httpx 异常文本含完整 URL；`exchange_code`/token 获取把 `secret` 放 params 同理；`process_outbox` 把 `str(exc)` 原样写入 `last_error`，经 `GET /notifications/outbox/failed` 下发给持 `notification.retry` 者并在后台渲染。机制为存量，本批新增 `INVITE_CREATED` 事件使该路径流量常态化。修复：`last_error` 只落截断的错误类型/枚举码；凭据移出 URL 或入库前正则清洗；出参脱敏 | 7（代码链路逐段实证） | 红队 |
| N4 | `tests/test_invite_binding_postgres_concurrency_e2e.py:279` 附近 | **W1/I8 行锁整改无有效测试锁定**：并发重复撤销、撤销×消费两个 e2e 在测试体内手写「revoke 路由逻辑镜像」/「`_consume_invite` 条件更新镜像」，不调用生产代码；且镜像已与实现漂移（镜像只查 `revoked_at`，生产还校验 used/expires）。删掉路由里的 `with_for_update` 或调换校验顺序，全部测试仍绿。修复：镜像像同文件 I5 测试一样直接以 PG session 调用生产函数，镜像删除 | 8（双源） | 测试+可维护性（双确认） |

### P2

| # | 位置 | 问题 | 置信 | 来源 |
|---|---|---|---|---|
| N5 | `routers/auth.py:487-498` + `auth_service.py`（先查后插） | **callback 只捕 `AppError`，非 AppError 异常绕过失败审计与 302 契约**：同 openid 并发绑不同公司在 `wechat_identities.openid` 唯一约束抛 `IntegrityError` → 全局 handler 裸 JSON 409，微信浏览器看不到状态页、不落 `WECHAT_OAUTH_CALLBACK_FAILED`——最典型的并发绑定失败零留痕；commit 阶段其他异常同理。与未修复的 I14/P2-4② 同根。修复：callback 再包 `except Exception`（IntegrityError 特判「并发重复绑定」业务码），统一 rollback/审计/固定 302；补同 openid 跨公司并发测试 | 9（双源） | Codex+红队（双确认） |
| N6 | `routers/auth.py:366-384`（confirm-start）+ `:451-467`（start） | **通道故障处理只覆盖 callback，两个 start 阶段是同类缺口**：`/wechat/start` 被浏览器直接导航，`WECHAT_NOT_CONFIGURED`/`WECHAT_SCOPE_INVALID` 时用户看到裸 JSON（后者无前端文案）；`confirm-start` 在调用 `authorization_url()` **之前**就提交 `INVITE_CONFIRM_START` 审计——配置错误时留下「已开始」的成功审计却没产生跳转，且无通道 error 日志。修复：两处统一包通道异常、落失败审计与告警并跳固定状态页；授权 URL 成功后再提交审计（或记 `INVITE_CONFIRM_START_FAILED`） | 10（顺序逐行实证） | Codex |
| N7 | `outbox_worker.py`（重试语义）+ `routers/notifications.py:56` | **每条邀请产生一条生产必败的 outbox**：未配模板固定 `TEMPLATE_NOT_CONFIGURED`、配了模板固定 `NO_RECIPIENT`（未绑定无 openid），均按通用失败重试 5 次退避后转 `DEAD`，持续污染失败率；后台失败面板默认 `status=FAILED`，转 `DEAD` 后反而从视图消失。修复：无可投递收件人不进发送 outbox（改审计事件或 `MANUAL_ACTION_REQUIRED` 终态）；确定性错误不重试；DEAD 进默认运维视图 | 10 | Codex |
| N8 | `routers/auth.py:49-65` | **限流桶无界增长 + 清扫放大**：限流键含未校验的原始 invite 且先于邀请校验执行，攻击者提交不同垃圾 token 即不断建桶；超 4096 后每个请求全量 O(n) 扫描未过期键（清理只删全过期键，不缩容），内存/CPU 双重放大。修复：键哈希归一到固定长度、按桶数上限硬拒或 LRU 淘汰；无效邀请另设全局/IP 粗粒度限流 | 9（双源） | Codex+红队（双确认） |
| N9 | `auth_service.py:315-344` + `admin/app.js`（邀请记录） | **使用者归因仍以当前主账号冒充历史**：接口从当前 `company.primary_user_id` 取名硬挂到 `used_at` 最新邀请上；主账号因数据修复/换绑变化后，历史邀请显示新账号为使用者（错误取证归因）。`WECHAT_BIND` 审计已有 `invite_id` 可关联真实用户但列表未用。修复：消费邀请同事务持久化不可变 `used_by_user_id`，或按审计关联；无法证明的显示「未记录」 | 9 | Codex |
| N10 | `scripts/scheduler.py:55-77` | **调度周期单事务末尾 commit**：outbox 外发（消息已真实发出）与 slow/hourly 作业同事务，任一后续作业抛错 rollback 整周期，outbox 行退回 PENDING → 下周期重发；故障作业持续失败时对用户无限重复推送并烧微信配额（存量结构，本批 `INVITE_CREATED` 放大面）。修复：`process_outbox` 单独事务提交（或按条目提交），外发后立即持久化 SENT | 7（结构实证，未运行动态验证） | 红队 |
| N11 | `routers/auth.py`（callback except 分支） | **失败审计自身是无限流的匿名 DB 写原语**：`GET /auth/wechat/callback?code=x&state=<垃圾>` 每请求落一行 `WECHAT_OAUTH_CALLBACK_FAILED`（rollback+insert+commit），无需有效邀请/state；I15 只限流了 confirm-start，回调路径无应用层节流，分布式打点即可写放大撑爆审计表——I15 要封的「匿名刷审计」在新路径复发。修复：按真实来源 IP 粗粒度限流/短窗去重/采样聚合，无效 state 先落结构化日志不落库 | 7（双源） | 红队（主审核验代码路径成立） |

### P3（改进项）

| # | 位置 | 问题 | 置信 | 来源 |
|---|---|---|---|---|
| N12 | `test_wechat_oauth.py:719`、`:567`、`test_auth_company.py:586` | 测试缺口：通道告警仅 `WECHAT_OAUTH_UNAVAILABLE` 一码有日志断言（删另两码或改任意 5xx 告警不会红）；`flow=='login'` 分支无审计断言；限流负例缺（429 HTTP 状态码本身未断言、60s 窗口恢复未测、4096 清理分支零覆盖）；`test_confirm_start_neutralizes...` 恰好打满 10 次额度，与限流常量形成隐藏耦合且桶为跨测试全局态无清理 fixture | 6-8 | 测试 |
| N13 | 多处 | 可维护性（可维护性专家 16 条归并）：通道故障码在两个相邻 frozenset 整段重复、前端 `noCta/homeCta` 与 `AUTH_ERROR_META` 平行集合、CTA 同步测试硬编码三码（清单第四份拷贝）；`revoke_invite` 业务滞留路由层（N4 的根因）；`AUTH_INVITE_INVALID` 三元组七连抄；`find_company_by_contact_phone`/`bind_wechat_by_invite` 零生产调用死代码且前者零测试；双参 AND 语义双实现；I19 helper 复制加重；`list_company_invites` docstring 未随 P3-6 更新；限流 4096 魔法数未命名 | 6-9 | 可维护性 |
| N14 | `test_v12_dispatch_claim.py:216` + 文件头 imports | **存量测试基建两缺陷（非本批引入，四文件 `git diff` 为空）**：① `workdays_between` 断言依赖「当天是工作日」，UTC 周六/周日全套件必红（本评审日实证 `assert 2 == 3`）；② 该文件不导入打 mapper 补丁的 `reward_models_v12`，单文件运行必 `TypeError`（依赖字母序在前的 `test_pre_go_live_security.py` 先导入）。另有根 `pyproject.toml` `--maxfail=1` 使首个失败掩盖后续。修复：测试改固定日期或 monkeypatch 日历；文件头补 `import reward_models_v12`（同其他三个文件）；CI 建议去 `-x` | 9（主审复现实证） | 主审 |

## 5. 测试执行证据（661606a 干净检出，主仓 .venv）

| 范围 | 结果 |
|---|---|
| 批次相关 8 文件（wechat_oauth / auth_company / frontend_contract / binding_integrity / notifications / security_gate / pre_go_live_security_review / invite_binding_pg_e2e） | 109 passed，5 skipped（PG 专项无 `V12_E2E_DATABASE_URL` 按设计 skip），0 失败 |
| 完整套件（`pytest --maxfail=0`） | **539 passed，7 skipped，1 deselected，0 失败**；deselect 的即 N14① 周末日期缺陷例 |
| N14 归属性核验 | `git diff 919fe1c..661606a -- test_v12_dispatch_claim.py workday_calendar.py reward_models_v12.py dispatch_v12.py` 输出为空——两缺陷均为存量 |

## 6. 未修复问题清单（汇总，按优先级）

**完全未修复（前两轮已提出）**

1. **P2-2**：OWNER/FINANCE 仍可见「邀请/邀请记录」按钮，点击必 403（`admin/app.js:46`，`can()` helper 在旁不用）。
2. **P2-4①**：callback 缺 `code/state` query 仍被 FastAPI 422 裸 JSON 拒绝（`routers/auth.py:471-473`）。
3. **P2-4② / I14**：同 openid 并发回调撞唯一约束 → 未处理 `IntegrityError` → 裸 JSON 409、无审计（即 N5）。
4. **I19**：测试 helper 复制未收敛，本批又添三处（N13 内）。
5. **I21**：PG 并发 e2e 对空库单跑仍 ValueError 而非 skip。

**部分修复（有实质残留）**

6. **I15 限流可绕过**：`x-real-ip` 客户端可控 + 进程内无锁字典（N1）+ 桶无界增长（N8）——「有限流」但持邀请者/匿名者仍可刷审计。
7. **I16 核查未接入**：`binding_integrity.py` 零生产调用点，脏数据仍可上线（N2）。
8. **P2-1 的 start 阶段缺口**：`/wechat/start`、confirm-start 通道故障裸 JSON + 审计先于 `authorization_url` 提交 + 无告警（N6）。
9. **P3-6 归因残留**：邀请记录仍以「当前主账号」冒充历史使用者，换绑后取证归因错误（N9）。
10. **P2-3/I3 残留**：`safe-html.js`/`enhancements.js` 等仍无版本参数机制。
11. **P3-8 残留**：`WechatOAuthClient`×3、`RedirectResponse`×2 局部导入未提升。

**新发现待整改（本批引入或暴露）**

12. **N3（P1-安全）**：outbox `last_error` 持久化含 `access_token`/`secret` 的异常原文并经 failed-outbox API 回显。
13. **N4（P1-测试）**：PG 并发撤销 e2e 用已漂移的镜像逻辑，行锁与生命周期校验整改无有效锁定。
14. **N7**：`INVITE_CREATED` outbox 生产必败仍重试 5 次转 DEAD，DEAD 不在默认失败视图。
15. **N10**：scheduler 单事务回滚致已发送消息重复推送。
16. **N11**：callback 失败审计匿名无节流，可被刷写。
17. **N12/N13/N14**：测试与可维护性改进项（含两处存量测试基建缺陷，周末必红 + 单文件必败）。

## 7. 建议行动（按优先级）

1. **N3**：凭据清洗/脱敏——安全缺陷，涉及真实密钥落库与运营端回显，最先修。
2. **N1+N8+N11**：限流与审计写放大一起收口（可信 IP 解析 + 共享/有界计数 + callback 失败审计节流）。
3. **P2-2**：按 `can('*')` 隐藏邀请入口（一处前端改动，半小时级）。
4. **N5/P2-4/I14**：callback 适配层统一包住 422 与非 AppError 异常（含 IntegrityError 幂等重读），一次改完三个未决项。
5. **N6**：两个 start 阶段的通道异常处理与审计顺序。
6. **N2**：`binding_integrity` 接入发布/启动门禁与告警；**N4**：并发 e2e 改调生产代码。
7. **N7+N10**：outbox 必败路径终态化 + scheduler 事务拆分。
8. **N9、N12–N14、I19/I21** 随迭代收敛；N14 的周末红测试建议立即修（避免每个周六的全量 CI 假红掩盖真回归）。

---

## 8. 整改批次记录（2026-08-23，§4–§6 全部未决项收口）

按 §7 优先级分十批（B0–B9）TDD 整改（每批先 RED 后 GREEN、相关回归全绿、中文提交）：

| 批次 | 提交 | 修复项 | 关键实现 |
|---|---|---|---|
| B0 | `c5b354a` | P2-4/I14/N5 | callback 适配层统一收敛一切失败为 302（含缺参与非 AppError 转译），同 openid 并发撞唯一约束时幂等重读重放 |
| B1 | `dcaf944` | N3 | `scrub_credentials` 写路径脱敏 outbox `last_error`（保留异常类名），failed-outbox 出参兜底打码存量脏数据 |
| B2 | `1ee94b4` | I15/N1/N8/N11 | `trust_proxy_headers` 默认关（不信任 x-real-ip）；限流键 SHA-256 定长 + 4096 桶 fail-closed；callback 失败审计按 IP+reason+根因异常类节流；conftest 清桶 fixture 消除跨测试全局态 |
| B3 | `a7ade27` | P2-2 | 管理端邀请/邀请记录入口按 `can('*')` 门控（与后端三接口权限口径对齐），契约测试锁定 |
| B4 | `9887374` | N6 | `/wechat/start` 与 confirm-start 通道故障 302 收敛 + 通道告警 + 失败审计（action/stage 区分）；`INVITE_CONFIRM_START` 只在 authorization_url 成功后落审计 |
| B5 | `4d68523` | N7/N10 | 确定性投递失败（无模板/无收件人）终态化 `MANUAL_ACTION_REQUIRED` 不再空转重试；失败面板默认三态；scheduler 每 job 落库 outbox 进度防重复推送 |
| B6 | `042e240` | N2/N4/I21 | `PRIMARY_WITHOUT_IDENTITY` 升 error，接入 scheduler 日检告警 + `scripts/check_binding_integrity.py` 门禁；撤销邀请下沉 `revoke_company_invite` 供 e2e 直测生产实现；e2e 空库显式 skip |
| B7 | `972735b` | N9 | `InviteToken.used_by_user_id` 消费同事务真实归因（迁移 0008），列表不再按当前主账号猜测，历史行显示「未记录」；换绑不漂移测试锁定 |
| B8a | `0c60e9a` | N12/N14 | 通道告警断言由 `_WECHAT_CHANNEL_FAILURE_CODES` 集合驱动参数化（变异验证：禁用告警四用例全红）+ login 流审计 flow 字段；`test_v12_dispatch_claim.py` 补 `reward_models_v12` 导入（单文件可运行）+ `workdays_between` 断言改北京时区口径（周末全量假红根除） |
| B8b | `8203ffc` | N13/P2-3/P3-8/I19 | 通道码两 frozenset 并集归并；H5 CTA 并入 `AUTH_ERROR_META` 第三元素（契约测试集合驱动，消灭第四份拷贝）；validate/consume 双参 AND 语义收敛 `_invite_matchers`；删 `find_company_by_contact_phone` 死代码；断言/建司/admin 登录样板收敛单一来源；auth.py 六处局部 import 提升；两个 index.html 版本参数递增至 `20260823-p3` 并补齐裸引用 |
| B9 | — | 终验 | 全量回归（不再 deselect `test_v12_dispatch_claim`），评审记录收口 |

**遗留说明**：`bind_wechat_by_invite` 保留（I13 显式测试缝，docstring 声明生产路由已全部走 invite_id，行为与生产通道同构）；`--maxfail=1` 根配置维持（全量终验显式 `--maxfail=0`）。

---

*本记录由 Claude Code 评审流程生成：主审 + 测试/可维护性/红队三个并行专家子代理 + Codex 独立对抗评审；P2-2/P2-4/I14/I16-N2/N3/N6 等关键判定均经主审在 661606a 提交态逐行实证，外部主张未经核验者已标注置信度与来源。全部验证在 detached worktree 干净检出上执行，未受主仓并行会话未提交改动影响。*
