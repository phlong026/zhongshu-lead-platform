# 40 - V1.2 T02 工作日历服务评审

- 任务：T02
- 结论：通过
- 范围：工作日计算、维护/导入 API、审计、测试

## 评审结果

- [x] 申诉截止和奖励截止共用 `WorkdayCalendarService`。
- [x] 显式日历优先于周一至周五兜底，可表达法定节假日和调休工作日。
- [x] `add_workdays` 保留领取时刻的时分秒与时区信息。
- [x] 管理员可查询、单日维护和批量导入；范围与批量大小受限。
- [x] 单日修改记录 before/after 审计；批量导入记录数量和日期范围。
- [x] 批量导入重复日期在写入前拒绝，避免部分成功。
- [x] 覆盖跨周末、节假日、调休和日期区间测试。

## 业务口径

`add_workdays(claimed_at, 3)` 不计领取当日，从后续日期开始累计三个工作日；V1.2 领取事务将结果同时固化到申诉和奖励截止字段。
