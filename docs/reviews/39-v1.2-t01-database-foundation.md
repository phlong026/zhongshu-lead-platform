# 39 - V1.2 T01 数据库迁移与领域数据基础评审

- 任务：T01
- 结论：通过
- 范围：V1.2 扩展字段、新表、索引、约束、迁移检查点、CI 迁移门禁

## 评审结果

- [x] `0002_v12_foundation` 从 `0001_initial` 前向升级。
- [x] 既有 `leads`、`assignments`、`verification_tasks`、`return_requests` 采用增量字段，不破坏 V1.0.1 数据。
- [x] 新增工作日历、公司能力、服务区域、去重事件、覆盖记录、供应商奖励和迁移检查点。
- [x] 自供/接收公司、申诉截止、奖励截止和任务类型具备结构化字段。
- [x] 奖励与派发一对一唯一约束；奖励积分非负约束。
- [x] SQLite 升级→降级→再升级迁移测试通过。
- [x] 应用开发环境 `create_all` 显式加载 V1.2 模型；Alembic 环境不提前加载 V1.2 元数据，避免历史 `0001` 抢先建表。

## 发现并修复

1. SQLite 批量新增外键要求约束命名：已为所有既有表新增外键设置稳定名称。
2. 降级前必须移除引用新增列的索引：已显式按表删除。
3. 历史 `0001` 使用 `Base.metadata.create_all`，若 Alembic env 提前导入 V1.2 模型会导致新表重复：已保持 env 仅加载 V1.0.1 模型，由 `0002` 独立负责 DDL。

## 遗留风险

生产 PostgreSQL 迁移仍需在上线前使用脱敏快照执行 T33 迁移演练；不影响本任务合并。
