# V1.2 P1 基础设施准备评审（Issue #42）

- 关联 Issue：#42（Go-Live V1.2 P1 基础设施、域名与对象存储）
- 父任务：#48（生产上线总控）
- 分支：`chore/v1.2-p1-infrastructure`
- 风险等级：P0（生产基础设施）
- 当前状态：本地可执行工作完成并通过本地 CI Contract；真实云资源未提供，阶段标记为 `BLOCKED-PENDING-EXTERNAL`

## 交付内容

| 文件 | 作用 |
|---|---|
| `scripts/verify_infrastructure.py` | 宿主机基础设施验证：主机规格、Docker/Compose、PostgreSQL16 暴露与持久化盘、NTP/时钟、公网监听、TLS、私有对象存储加固、环境隔离；输出脱敏 JSON 证据 |
| `scripts/bootstrap_production_host.sh` | 幂等主机引导：Docker/Compose、数据盘挂载（仅在显式 `FORMAT_DATA_DISK=1` 时格式化）、chrony NTP、ufw 22/80/443、目录布局 |
| `docs/runbooks/V1.2_P1_INFRASTRUCTURE.md` | P1 执行 runbook：#42 验收项→命令/证据映射、外部前置清单、执行步骤、NO-GO 规则 |
| `production-evidence/README.md` | 证据归档规则（脱敏、可复现、失败证据保留） |
| `production-evidence/p1-infrastructure-evidence.template.json` | P1 证据 JSON 模板 |
| `apps/api/tests/test_v12_p1_infrastructure.py` | 验证脚本纯函数单元测试（13 例） |

## 自动检查（本地 CI Contract 等价执行）

- `python -m compileall -q apps scripts migrations`：通过；
- `python scripts/secret_scan.py`：通过（无已提交 Secret）；
- `python scripts/check_js.py`：通过（29 个 JS 文件）；
- `python -m pytest -q`（完整套件）：通过（含 13 例新增测试）；
- `python scripts/export_openapi.py`：通过；
- Alembic SQLite 迁移冒烟（up/down/up）：通过；
- `git diff --check`：通过；
- `python scripts/package_release.py --version V1.2.0-rc`：安全门禁要求先提交（预期行为，提交后复验）；
- `bash -n scripts/bootstrap_production_host.sh`：语法通过；
- `python scripts/verify_infrastructure.py --environment staging`：真实 Docker daemon 下运行成功，正确识别本地 staging PostgreSQL16 仅回环发布 5432、无公网暴露，退出码 0。

## 安全评审要点

- 验证脚本内置脱敏：所有含 SECRET/PASSWORD/TOKEN/KEY/CREDENTIAL 的环境变量值及 `DATABASE_URL` 密码均不进入证据/回显；
- 真实 Secret 不写入仓库、Issue、PR；`.gitignore` 已排除 `.env`、`infra/certs/*`、`backups/*`；
- `verify_infrastructure.py` 只输出非敏感证据字段（规格、版本、端口、同步状态、证书剩余天数、桶加固状态）；
- bootstrap 脚本仅在显式 `FORMAT_DATA_DISK=1` 时格式化数据盘，默认幂等且非破坏；
- 公网监听检查强制仅放行清单端口（默认 80/443），PostgreSQL 5432 公网暴露即 NO-GO。

## 评审结论

- 代码/脚本结构与仓库既有约定一致（`scripts/` 工具、`docs/runbooks/` runbook、`docs/reviews/` 评审记录）；
- 无未解决 P0/P1/P2 问题；
- 遗留：真实腾讯云资源（4C8G CVM、数据盘、私有 COS、域名/DNS/TLS、安全组）未提供，需外部输入到位后执行 bootstrap + verify 并归档证据，再关闭 #42。

## 评审签字

| 角色 | 结论 | 签字/时间 |
|---|---|---|
| 技术（Codex 执行） | 本地工作完成，待外部资源 | 2026-08-12 |