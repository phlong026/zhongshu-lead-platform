# V1.2 Hardening H06：测试覆盖率门禁最终评审

- Hardening Issue：#55
- 父任务：#49
- 分支：`chore/v1.2-h06-coverage-gate`
- 风险等级：P2
- 最终代码门禁：Main Release Verification run `31263021557`
- 结论：**PASS / Ready to Merge**

## 本次实现

1. Main Release Verification 的 backend tests 改为 pytest-cov line + branch coverage；
2. 输出 terminal / XML / JSON / critical JSON；
3. `check_coverage_gate.py` 独立计算 line 与 branch，避免 branch 模式下把综合百分比误当 line；
4. critical modules 单独聚合 auth / RBAC / points / return / reward / dispatch service+router；
5. global 与 critical 均形成强门禁；
6. coverage artifact 保留 30 天；
7. pytest 即使在 report 生成前失败，也始终落盘 output + exit-code，保留失败证据；
8. 不 omit H05 安全负例或高风险运行模块。

## 第一轮真实基线

Main Release Verification run `31262652850`：

- global line：79.23%；
- global branch：54.99%；
- pytest-cov combined：74.40%。

第一版 critical 集合遗漏 `services/dispatch_v12.py`。下载该 run coverage artifact 后，按完整 critical 集合重算：

- critical line：85.93%；
- critical branch：64.87%。

第一轮故意以 global branch 75% 强门槛运行并被正确阻断。当前 branch 与建议目标存在约 20 个百分点差距，不能通过一次性堆低价值测试把 main 变成形式化绿灯。

## 最终强门禁

锁住当前真实基线，防止覆盖率回退：

- global line ≥79.0%；
- global branch ≥54.5%；
- pytest-cov combined ≥74%；
- critical line ≥85.5%；
- critical branch ≥64.5%。

长期提升目标继续为 global branch ≥75%、critical line/branch ≥90%。后续补测试必须优先异常、权限、并发、幂等和边界路径。

## Critical 实测

- auth.py：line 90.91%、branch 83.33%；
- auth_service.py：line 86.13%、branch 66.00%；
- rbac.py：line 97.14%、branch 87.50%；
- points_service.py：line 90.63%、branch 70.00%；
- return_v12.py：line 81.79%、branch 55.93%；
- supplier_reward_v12.py：line 86.13%、branch 62.07%；
- dispatch_v12.py：line 90.09%、branch 68.92%；
- v12_dispatch.py：line 69.23%、branch 50.00%。

## Review 发现与闭环

Codex 首轮发现并闭环 3 项：

1. **P1** critical 90% 只有展示、没有实际门禁 → 增加 critical line/branch 强门禁并锁定实测 baseline；
2. **P2** dispatch critical 集合遗漏 `services/dispatch_v12.py` → 纳入集合并重新计算 baseline；
3. **P2** coverage report 缺失时可能没有 artifact 证据 → pytest 始终写 output/exit-code，artifact `always()` 上传。

3 个 Thread 均已逐条回复并 resolved。

收口复审已针对最新代码发起；截至最终收口阶段未产生新增 Review Thread。结合首轮 finding 全闭环、latest 双 CI 全绿与 latest diff 人工审核，未发现新的 P0/P1/P2。

## 门禁证据

Main Release Verification run `31263021557`：

- backend tests with coverage：success；
- global/critical coverage gate：success；
- coverage artifact upload：success；
- PostgreSQL migration：success；
- OpenAPI / JS / secret scan / compile / whitespace / metadata：success。

## 最终结论

H06 已形成可回归、可追踪、可阻断的 line + branch coverage 门禁，并对高风险核心建立更高 baseline floor 和 90% 明确提升目标。

**结论：PASS，允许 squash merge 到 `main`。**
