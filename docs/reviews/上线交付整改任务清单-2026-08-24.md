# 上线交付整改任务清单

日期：2026-08-24

目标：把当前 V1.2 分支从“本地可验收”推进到“可交付业主试用/灰度”的状态。状态只记录本仓库已完成的本地修复；真实生产域名、微信服务号、业主 UAT 仍是外部门禁。

## P0 必改

- [x] 退回证据口径：后端、H5 和合同测试统一为“沟通截图或电话录音任一类型满足即可”。
- [x] 公司快速创建：不能默认把接单资质和服务地区设为已审核通过，避免绕过业主要求的申请/审核闭环。
- [x] 暂存清理：移除线上物理删除入口和豁免，仅保留只读概览，避免误删正式客资数据。
- [x] 发布元数据：版本号、环境样例、发布清单和变更记录与本轮修复保持一致。

## P1 上线前加固

- [x] 调度频率：每日类任务按 24 小时周期执行，避免 12 小时重复跑。
- [x] 绑定完整性：上线预检或调度链路要覆盖主账号/邀请绑定完整性检查。
- [x] 地区匹配：父级城市已批准服务范围可覆盖其直属区县客资，并补足候选匹配回归测试。
- [x] 静态/安全扫描：仅扫描 Git 已跟踪文件，过滤虚拟环境和缓存目录，并覆盖核心生产密钥名。

## 外部门禁

- [ ] 代码合并与远端门禁：合并到 `release/v1.2.1`，确认 PR CI、Release CI、PostgreSQL 迁移和浏览器任务全部通过。
- [ ] 不可变镜像：安全扫描通过后推送 `1.2.1@sha256:<digest>`，归档 `scan-subject.json`、镜像 ID、摘要和 SBOM。
- [ ] 正式配置与基础设施：完成服务器、DNS、TLS、Nginx、PostgreSQL 16、私有 COS Bucket、最小权限 CAM 和生产 Secret 托管。
- [ ] 对象存储验证：在正式 COS 上完成 put/head/get/delete canary，确认截图、录音上传和临时授权下载可用。
- [ ] 数据迁移：冻结旧写入并备份，完成隔离预演、Alembic 升级、V1.2 回填、对账和绑定完整性检查，所有结果归档。
- [ ] 灾备与回滚：完成 PostgreSQL 恢复演练、对象存储恢复确认、应用回滚和既定 RTO 验证。
- [ ] 微信服务号认证与网页授权域名 `zszhj.cn` 配置完成后，做真机微信登录回调验收。
- [ ] 监控告警：验证 API、Scheduler、PostgreSQL、磁盘、5xx、Outbox、积分差异和备份失败告警可触达责任人。
- [ ] 业主 UAT：PC 管理后台、H5 工作台、内部电销页按业务流程走通。
- [ ] 灰度发布：选择 3–5 家加盟商运行 24–72 小时，确认无 P0、账务差异、重复派发、越权或恢复失败。
- [ ] 交付签字：产品/平台、业务运营、财务/积分、技术四方确认生产证据包后，才能标记 `PRODUCTION_GO`。
