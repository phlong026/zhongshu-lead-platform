# 本地 PR 与代码评审：客资 AI 系统问题反馈 V3

- 评审时间：2026-08-29T00:55:00+08:00
- 目标分支：`main`（基准 `23ef902` 的后续本地主分支）
- 来源分支：`feat/customer-feedback-v3`
- 本地 PR 差异：`main...feat/customer-feedback-v3`
- 总体结论：通过，可合并到本地 `main`
- 明确延后：第 6 条“真实公众号名称和菜单路径”暂不写死，待运营提供真实值后单独修改和真机验收

> 分支从当时已验证的 `82cf73c` 工作基线创建，因此对 `main` 的本地 PR 同时包含 `d680cf2..82cf73c` 四个既有账号绑定、清理边界和安全验证收口提交。本次评审与合并范围以完整 `main...feat/customer-feedback-v3` 为准。

## 一、需求落地结果

| # | 确认需求 | 本次结果 |
|---:|---|---|
| 1 | 独立退回记录入口 | 管理端和加盟商工作台提供独立退回列表/详情，不依赖客资详情入口。 |
| 2 | 密码是公众号登录之外的备用方式 | `/auth/me` 返回 `has_password`；首次可无旧密码设置，后续修改必须验证当前密码。 |
| 3 | 候选公司改为卡片 | 派发候选以可对比的公司卡片展示区域、积分、能力和排除原因。 |
| 4 | 手机号和客户授权均为正式提交必填 | 平台与加盟商客资共用服务端强校验，其他字段保持可选。 |
| 5 | 拒绝领取/发起退回/确认无效分开统计 | 公司列表与经营看板分别输出 `refused_claim`、`return_requested`、`confirmed_invalid`。 |
| 6 | 真实公众号名称和菜单路径 | 按确认暂缓；本次未伪造或写死真实运营值。 |
| 7 | 站内消息 + 微信公众号消息 | 核心业务事件同时创建站内消息与 Outbox，退回消息统一由审计投影生成，避免重复。 |
| 8 | 完整供客直接进派发；运营确认完成后不留待办 | 有可派发地区的完整客资直达 `READY_DISPATCH`；仅资料不全时派电销；运营处置完成任务转为 `RELEASED`。 |
| 9 | 系统必须到乡镇级 | 平台录入、加盟商供客和公司服务区域均支持省/市/区县/乡镇层级校验与保存。 |
| 10 | 公司关键信息完整范围 | 公司名称、负责人、电话、等级、备注、服务区域、接客/供客开关已贯通详情与编辑。 |
| 11 | 不需要删除，已流转客资不得物理消失 | 移除公司 DELETE API、请求体和物理 purge 服务；草稿仍只能在未流转前丢弃；已流转客资、账号申请、服务区域和业务历史均保留。 |
| 12 | 负责人看公司总数，员工只看本人；消费金额改消耗积分 | 报表根据角色限定数据范围，页面和返回字段统一为“消耗积分”。 |

## 二、评审中追加的可交付性修正

- CSV 导出支持来源、接收公司、客资/派发/退回状态筛选，并包含派发单、退回单唯一编号、处理结果与最后处理时间。
- CSV 用户文本对 `=`、`+`、`-`、`@ 及制表/换行开头做公式注入防护。
- 退回列表改为批量读取 Lead、Assignment、User、Task 和 Reward，避免按行 N+1 查询。
- 积分到账/余额预警消息链接改到现行 `/h5/v12-workbench.html?view=points`。
- 服务区域撤回保留审核状态行，不再删除历史。

## 三、代码评审结果

1. 独立架构复审：首轮 `BLOCK`，指出乡镇录入、退回通知重复、历史数据删除和 CSV 注入风险；逐项整改后最终 `PASS / Architectural Status: CLEAR`。
2. 独立综合代码评审：第二轮对客资流转、导出、积分深链、退回批量序列化和服务区域复核结论为 `APPROVE`；最终移除公司 DELETE API 后的增量复审再次结论为 `APPROVE`，无 P0/P1/P2。
3. 自审：已检查完整 `git diff`、物理删除路径、敏感信息、外部调用错误边界、导出安全和列表查询复杂度；无遗留 P0/P1/P2。

## 四、验证证据

- 全量后端测试（提交前排除“工作区必须已提交”专用门禁）：833 项收集，命令退出码 0；其中 10 项目标环境门禁按条件 skip。
- 变更相关集成/合同测试：命令退出码 0。
- JavaScript 语法：`scripts/check_js.py` 通过，9 个文件。
- Python 编译：`python -m compileall -q apps scripts migrations` 通过。
- 密钥扫描：`scripts/secret_scan.py` 通过，无已跟踪密钥。
- 依赖健康：`pip check` 通过；`pip-audit` 无已知漏洞。
- 补丁格式：`git diff --check` 通过。

## 五、合并与回滚

- 本地合并使用 `--no-ff`，保留本地 PR 边界和独立分支历史。
- 合并后在单独干净 `main` worktree 重跑全量测试和静态检查。
- 无数据库迁移、无新依赖。若需回滚，对本地 merge commit 执行 `git revert -m 1 <merge-commit>`。

## 六、未在本地代码完成范围内的门禁

- 第 6 条的真实公众号名称、菜单路径与最终跳转 URL。
- 真实公众号 OAuth、OpenID 绑定、模板消息到达和 iOS/Android 微信真机深链。
- 正式 PostgreSQL 数据量、对象存储、恢复演练、真实加盟商 UAT 与灰度。
