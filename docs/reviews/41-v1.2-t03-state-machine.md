# 41 - V1.2 T03 状态机与历史映射评审

- 任务：T03
- 结论：通过
- 范围：客资、退回、奖励状态枚举，转换守卫，历史只读映射

## 评审结果

- [x] 平台手工客资允许 `DRAFT → READY_DISPATCH`，不经过前置电销。
- [x] 供应商客资支持 `DRAFT → PENDING_REVIEW → READY_DISPATCH`。
- [x] 退回申请必须 `SUBMITTED → VERIFYING → REVIEWING`，禁止跳过电销事实核验直接终审。
- [x] 奖励在 `OBSERVING` 期间可冻结、结算或取消；`REVERSED` 仅允许从 `SETTLED` 进入。
- [x] 同状态幂等写入允许，非法跨级转换抛出明确异常。
- [x] V1.0.1 历史状态映射仅用于读取兼容，不批量改写历史事实。

## 后续约束

T04-T25 的所有写服务必须调用状态转换守卫，不允许路由层直接写状态字符串。
