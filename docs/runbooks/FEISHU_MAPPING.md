# 飞书多维表格字段映射

默认读取字段：客户姓名、手机号、省、市、区县、地区编码、业务类目、品牌、来源渠道、客户需求、预算下限、预算上限、获客成本。

正式上线前应：

1. 锁定 `app_token` 和 `table_id`；
2. 为应用授予记录读取/更新权限；
3. 使用 `record_id` 作为同步幂等键；
4. 验证分页、Token 续期和限流重试；
5. 不使用表格行号作为唯一标识；
6. 字段变更先在测试表完成映射回归。
