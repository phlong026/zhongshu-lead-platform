# 微信公众号 Gate 0

开发或生产联调前完成：

- 企业主体服务号及认证状态；
- 网页授权域名和 OAuth 回调；
- `APP_BASE_URL` 使用 HTTPS；
- 微信消息能力、模板 ID、发送限制；
- 用户未关注、取消关注和消息发送失败的降级路径；
- iOS/Android 微信内打开、深链回跳与 Cookie 会话；
- 生产环境关闭 `WECHAT_DEV_MOCK`；
- 转发深链给其他公司账号时必须返回无权访问。
